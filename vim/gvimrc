if has('autocmd')
	" create & initialize new autocmd group 'misc'
	augroup window
		" clear group
		au!

		" set GUI window size automatically
		au GUIEnter *
					\ sleep 100m |
					\ set lines=52 columns=80
	augroup end
endif
